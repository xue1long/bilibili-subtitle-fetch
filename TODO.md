# 重构任务清单

## 已完成

- [x] 统一单视频、收藏夹 URL、UP 主空间入口
- [x] 新增来源适配器，统一输出 `Video` 对象
- [x] 保留 `subtitle_extractor.py` 兼容入口
- [x] 保留 Playwright 原生字幕和 ASR 兜底流程
- [x] 补充来源层测试并通过全量测试

## 按顺序执行

- [x] 新增统一 `SubtitleTask` 模型和任务状态枚举
- [x] 新增任务规划器，按 BV 号去重并识别成功、待处理、暂停、失败任务
- [x] 将统一 CLI 接入任务规划器，支持中断后继续
- [x] 抽离 Playwright 字幕后端，统一返回成功、无字幕、暂停、失败结果
- [x] 抽离 ASR 后端，统一处理音频临时文件和转录结果
- [x] 将收藏夹和空间同步结果保存为独立 manifest
- [x] 让 `download_list.json` 仅作为兼容输出，不再承担全部状态职责
- [x] 增加失败任务重试和按状态筛选命令
- [x] 增加单视频、收藏夹、空间、ASR 兜底端到端测试
- [x] 评估并迁移到 SQLite 状态库
- [x] 更新 README、AGENT.md、SKILL.md 和运行示例

## 执行约定

1. 严格按顺序完成，每项完成后运行相关测试。
2. 不删除已有字幕、音频救援脚本或旧命令入口。
3. 不覆盖同 BV 号的不同内容文件，冲突单独记录。
4. 每完成一项更新本文件状态。
